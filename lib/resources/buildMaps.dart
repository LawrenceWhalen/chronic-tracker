import 'package:flutter/material.dart';

class NavButtonBuildMap {
  Icon icon;
  String label;
  String path;
  bool isCurrentLocation;

  NavButtonBuildMap(this.icon, this.label, this.path, this.isCurrentLocation);
}